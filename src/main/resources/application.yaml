server:
  #Dodaję szczegóły dotyczące errorów w odpowiedzi HTTP
  error:
    include-binding-errors: always

spring:
  profiles:
    active: local
  #Wyłączam standardowy banner Springa
  main:
    banner-mode: OFF
#Tworzę własnego propertiesa
task:
  template:
    allow-multiple-tasks: true